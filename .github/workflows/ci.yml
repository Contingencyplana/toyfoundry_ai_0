name: CI
on:
  push:
  pull_request:
jobs:
  validate:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Validate templates and lanes config
        shell: pwsh
        run: |
          ./tools/ci/validate_templates.ps1
